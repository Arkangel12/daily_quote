import 'package:flutter/material.dart';

class AppColors {

  // Blue scale
  static const blueDarker = Color.fromRGBO(0, 133, 177, 1);//
  static const blueDark = Color.fromRGBO(0, 173, 225, 1);//
  static const blue = Color.fromRGBO(0, 204, 255, 1);//
  static const blueLight = Color.fromRGBO(96, 217, 255, 1);
  static const blueLighter = Color.fromRGBO(157, 228, 255, 1);//
  static const blueLightest = Color.fromRGBO(221, 248, 255, 1);//
  static const blueWhite = Color.fromRGBO(245, 253, 255, 1);//

  // Yellow scale
  static const yellowDark = Color.fromRGBO(255, 187, 5, 1); //
  static const yellow = Color.fromRGBO(253, 198, 50, 1); //
  static const yellowLight = Color.fromRGBO(255, 219, 95, 1); //
  static const yellowLighter = Color.fromRGBO(255, 228, 155, 1); //
  static const yellowLightest = Color.fromRGBO(255, 246, 221, 1);//

  // Pink scale
  static const pink = Color.fromRGBO(253, 50, 168, 1);//
  static const pinkLight = Color.fromRGBO(255, 107, 193, 1);//
  static const pinkLighter = Color.fromRGBO(255, 156, 213, 1);//
  static const pinkLightest = Color.fromRGBO(255, 222, 241, 1);//

  // Violet scale
  static const violetDark = Color.fromRGBO(106, 0, 209, 1); //
  static const violet = Color.fromRGBO(149, 46, 250, 1); //
  static const violetLight = Color.fromRGBO(177, 96, 255, 1); //
  static const violetLighter = Color.fromRGBO(207, 157, 255, 1); //
  static const violetLightest = Color.fromRGBO(238, 221, 255, 1); //

  // Cerice
  static const cerice = Color.fromRGBO(255, 53, 138, 1); //

  // Gray scale
  static const white = Color.fromRGBO(255, 255, 255, 1);
  static const whiteSmoke = Color.fromRGBO(245, 245, 245, 1);
  static const platinum = Color.fromRGBO(231, 231, 231, 1);
  static const gray = Color.fromRGBO(187, 187, 187, 1);
  static const darkGray = Color.fromRGBO(149, 149, 149, 1);
  static const black = Color.fromRGBO(46, 46, 46, 1);

  //Lime scale
  static const lime = Color.fromRGBO(39, 227, 143, 1); //
  static const limeLight = Color.fromRGBO(83, 242, 171, 1); //
  static const limeLighter = Color.fromRGBO(155, 255, 210, 1); //
  static const limeLightest = Color.fromRGBO(221, 255, 240, 1); //

  //Error
  static const error = Color.fromRGBO(255, 75, 75, 1); //
  static const warning = Color.fromRGBO(255, 141, 64, 1);

  // General Colors
  static const green = Color.fromRGBO(0, 181, 172, 1);
  static const whale = Color.fromRGBO(13, 73, 88, 1);
  static const raspberry = Color.fromRGBO(255, 53, 107, 1);

  // State colors
  static const attention = Color.fromRGBO(250, 200, 69, 1);

  static const success = lime;
  static const successLighter = limeLight;
  static const successLightest = limeLighter;

  // Opacity colors
  static const blackOverlayBackground = Color.fromRGBO(0, 0, 0, .5);
  static const blackOpacity = Color.fromRGBO(0, 0, 0, .2);
  static const whiteOpacity = Color.fromRGBO(255, 255, 255, .6);

}